# Django Production Environment Variables
# Copy this file to .env and fill in the actual values
# NEVER commit .env with real values to git!

# Django Core Settings
DJANGO_SECRET_KEY=your-super-secret-key-here-generate-a-new-one
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=your-app.up.railway.app,www.yourdomain.com

# Database Configuration (Railway automatically provides DATABASE_URL)
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# CORS Settings (Add your frontend domain)
CORS_ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://www.yourdomain.com

# Optional: Email settings (for password reset, etc.)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
DCPE_BASE_URL=https://something-something.up.railway.app